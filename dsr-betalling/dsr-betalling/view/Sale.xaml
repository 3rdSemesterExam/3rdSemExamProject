<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:dsr_betalling.view"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity" xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    x:Class="dsr_betalling.view.Sale"
    mc:Ignorable="d" FontSize="36">
    <!--<Page.TopAppBar>
        <CommandBar Height="45" >
            <CommandBar.Content>
                <Grid/>
            </CommandBar.Content>
            <AppBarButton Label="Sales" Icon="Globe"/>
            <AppBarButton Icon="Cancel" Label="Add Guest"/>
            <AppBarButton Icon="Accept" Label="DSR Menu"/>
            <AppBarButton Icon="Accept" Label="Log out"/>
        </CommandBar>
    </Page.TopAppBar>-->

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ListView x:Name="listView" Margin="0,105,870,0" Width="310" d:LayoutOverrides="TopPosition, BottomPosition" Background="#FFB62F2F" Height="525"/>
        <StackPanel x:Name="ControlStack" Margin="710,465,0,0" Orientation="Vertical" Width="200" Height="175">
            <Button x:Name="ConfirmButton" Content="Confirm" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" RenderTransformOrigin="0.608,2.209" Height="75" Width="200" Margin="0" FontSize="36"/>
            <Button x:Name="CancelButton" Content="Cancel" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" RenderTransformOrigin="0.608,2.209" Height="75" Width="200" Margin="0,25,0,0" FontSize="36"/>
        </StackPanel>
        <StackPanel x:Name="TopAppStack" Height="100" Margin="0,0,0,620" Orientation="Horizontal" Width="1280">
            <Button x:Name="SalesButton" Content="Sales" Margin="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Height="100" Width="320" FontSize="36" Padding="0">
                <!--<Interactivity:Interaction.Behaviors>
                    <Core:EventTriggerBehavior EventName="Click">
                        <Core:NavigateToPageAction TargetPage="dsr_betalling.view.Sale"/>
                    </Core:EventTriggerBehavior>
                </Interactivity:Interaction.Behaviors>-->
            </Button>
            <Button x:Name="AddGuestButton" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Height="100" Width="320" Content="Add Guest" FontSize="36" Padding="0">
                <!--<Interactivity:Interaction.Behaviors>
                    <Core:EventTriggerBehavior EventName="Click">
                        <Core:NavigateToPageAction TargetPage="dsr_betalling.view.AddGuest"/>
                    </Core:EventTriggerBehavior>
                </Interactivity:Interaction.Behaviors>-->
            </Button>
            <Button x:Name="DSRMenuButton" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="320" Height="100" FontSize="36" Content="DSR Menu" Padding="0"/>
            <Button x:Name="LogOutButton" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="320" Height="100" Content="Log Out" FontSize="36" Padding="0"/>
        </StackPanel>
        <StackPanel x:Name="PriceStack" Width="300" Margin="830,0,0,250" d:LayoutOverrides="TopPosition, BottomPosition" Orientation="Horizontal" Height="42">
            <TextBlock x:Name="SalesPriceBlock" Margin="0" TextWrapping="Wrap" Text="Pris i Kr." Height="42" Width="150"/>
            <TextBox x:Name="SalesPriceBox" Margin="0" TextWrapping="Wrap" Text="" Width="150" FontSize="36"  PlaceholderText="Dkk." BorderThickness="0" Height="42" MinWidth="7" MinHeight="0" Padding="0"/>
        </StackPanel>
        <StackPanel x:Name="AmountStack" Height="80" Margin="0,0,0,229" Orientation="Horizontal" Width="329">
            <TextBlock x:Name="AmountBlock" Margin="0,0,0,10" TextWrapping="Wrap" Text="Amount" Height="42" Width="150"/>
            <ComboBox x:Name="AmountComboBox" HorizontalAlignment="Stretch" Width="120" Height="30" VerticalAlignment="Stretch" Padding="0" VerticalContentAlignment="Stretch" Margin="50,0,0,0"/>

        </StackPanel>

    </Grid>
</Page>
